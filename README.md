Three Js Snowman Builder Game
========================

Snowman game built with three.js.

Clone repositry then:

`npm install`

Build the js/css
`npm run build `

To run the game:

`npm start`

Navigate to:

`localhost:8000`
